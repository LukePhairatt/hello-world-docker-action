name: test-all
on: [push]

jobs:
  build-and-test:
    runs-on: ubuntu-16.04
    steps:
    - uses: actions/checkout@v1
    - name: Build Docker Image
      uses: docker build -t test-docker-action . 

    - name: Do test
      run: docker run test-docker-action





